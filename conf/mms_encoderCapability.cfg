{
	"ffmpeg": {
		// max encodings like if only encodings will run. MMS decreases his values in case other processes are running
		"maxEncodingsCapability": 1,
		// max proxies like if only proxies will run. MMS decreases his values in case other processes are running
		"maxLiveProxiesCapability": 50,
		// max recordings like if only recordings will run. MMS decreases his values in case other processes are running
		"maxLiveRecordingsCapability": 15
	}
}
